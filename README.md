# gme product catalog scraper

## basic test setup
Run `npm install`
Run `npm run scrape`
The scraper will put the results in the `data` folder with the following naming convention:
`query`-`startingItem`-`endingItem`.json
